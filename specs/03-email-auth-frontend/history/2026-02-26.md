Tech-Stack: specs/00-tech-stack.md

# 2026-02-26 Progress Log

## Completed Today

- Bootstrapped `apps/web` (Next.js 15 + React 19 + TS strict + Tailwind v4).
- Implemented auth screens:
  - `login`, `signup`, `forgot-password`, `reset-password`, `verify-email`.
- Added `AuthAdapter` layer with:
  - `RestAuthAdapter` (active default)
  - `BetterAuthAdapter` skeleton (migration target).
- Added TanStack Query integration:
  - global provider
  - auth query/mutation hooks
  - session invalidation on login/logout.
- Added local development support:
  - Dev Auth floating panel (`Dev Auth Tools`)
  - local token handoff from signup/forgot flows.
- Added validation UX:
  - RHF + Zod messages
  - mode `onBlur`, revalidate `onChange`
  - inline field errors + `aria-describedby`/`role=alert`.
- Fixed style parity and token rendering issues:
  - `@repo/ui/styles.css` usage and Tailwind source path correction
  - explicit token classes where semantic utility mapping was missing.
- Fixed auth response shape mismatch:
  - `/auth/me` expects `data.user`.
- Added test coverage for web auth:
  - error map unit tests
  - adapter compatibility tests
  - login/reset flow component tests (Vitest + RTL).
- Added backend ops docs for email verification issue:
  - `specs/02-email-auth-backend/email-verification-runbook.md`.
- Updated backend/frontend email validation patterns to avoid deprecated Zod usage.

## Remaining Tasks

- FE-T030 Done checklist final closure and explicit FR evidence mapping.
- Optional: expand accessibility review beyond current auth forms.
- Optional: extend adapter migration tests when Better Auth integration begins.

## Tomorrow Resume Plan

1. Run manual sanity sweep:
   - signup -> verify -> login -> logout
   - forgot -> reset -> login
   - dev auth tools token flow
2. Confirm final checklist completion in `tasks.md`.
3. Start Better Auth migration slice (if requested):
   - wire real `BetterAuthAdapter` client methods behind `NEXT_PUBLIC_AUTH_ADAPTER=better`.

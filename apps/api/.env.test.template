# Test template for local/docker-based runs.
# Do not put real prod/stage/dev credentials here.

NODE_ENV=test
PORT=0

DATABASE_URL=postgresql://kami:ilovekami@localhost:5432/taste_spec_kit
REDIS_URL=redis://:ilovekami@localhost:6379

MAIL_TRANSPORT=log
MAIL_EXPOSE_TOKENS=true
APP_BASE_URL=http://localhost:3000
MAIL_FROM=no-reply@localhost

# Local development template only.
# Do not commit real secrets from prod/stage/dev servers.

NODE_ENV=development
PORT=3000

DATABASE_URL=postgresql://kami:ilovekami@localhost:5432/taste_spec_kit
REDIS_URL=redis://:ilovekami@localhost:6379

# Mail delivery mode: log | smtp
MAIL_TRANSPORT=log
# Set true only for local debug/testing; never enable in production.
MAIL_EXPOSE_TOKENS=false
# Frontend base URL used in verification/reset links.
APP_BASE_URL=http://localhost:3001
MAIL_FROM=no-reply@localhost
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
